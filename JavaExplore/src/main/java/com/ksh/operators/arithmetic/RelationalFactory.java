package com.ksh.operators.arithmetic;

public interface RelationalFactory {
	/**
	 * Returns true if left and right hand side are equal (Only for Reference)
	 * 
	 * @param number
	 *            to check is even
	 * @return result
	 */
	boolean isEvenNumber(long number);
	
	// Write the code which can cover the other Relational operator.
	
}
