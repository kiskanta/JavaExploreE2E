package com.ksh.operators.arithmetic;

public interface SchoolProblems {
	
	/**
	 * Area = PI * R *R  - where r is the radius of the circle and PI is 22/7.
	 * @param radius
	 * @return
	 */
	double areaOfCircle(int r);
	
	/**
	 * 
	 * @param a - base
	 * @param b - exponent
	 * @return power value
	 */
	long power(int a, int b);
}
